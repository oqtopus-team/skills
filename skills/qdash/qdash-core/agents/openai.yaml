interface:
  display_name: "QDash Core"
  short_description: "Connect to QDash through saved client profiles"
  default_prompt: "Use $qdash-core to connect to QDash through my local qdash-client profile."
